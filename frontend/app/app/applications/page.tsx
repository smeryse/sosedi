import Link from "next/link";
import { ArrowRight, Clock3, FileText, Plus, Sparkles } from "lucide-react";
import { PageFrame } from "@/components/tenant/page-frame";
import { demoProperties, formatRubles } from "@/data/demo";

export default async function ApplicationsPage({ searchParams }: { searchParams?: Promise<{ submitted?: string }> }) { const property = demoProperties[0]; const submitted = (await searchParams)?.submitted === "1"; return <PageFrame title="Мои заявки" description="Все отклики группы и ответы собственников — в одном понятном статусе." actions={<Link href="/app/housing" className="lime-button inline-flex items-center gap-2 rounded-full px-4 py-3 text-xs font-extrabold"><Plus className="size-4" /> Найти жильё</Link>}>{submitted ? <div className="mb-4 rounded-[18px] border border-emerald-200 bg-emerald-50 p-4 text-sm font-bold text-emerald-800">Заявка отправлена. Собственник получит сообщение группы.</div> : null}<div className="space-y-4"><Link href="/app/applications/application-34872" className="surface-card block p-5 transition-transform hover:-translate-y-0.5"><div className="flex flex-wrap items-start justify-between gap-3"><div className="flex items-start gap-3"><div className="grid size-11 place-items-center rounded-[14px] bg-[hsl(var(--accent-soft))]"><FileText className="size-5" /></div><div><h2 className="font-extrabold">{property.title}</h2><p className="mt-1 text-xs text-muted-foreground">Группа «Квартира в центре» · {formatRubles(property.price)} / мес.</p></div></div><span className="inline-flex items-center gap-1.5 rounded-full bg-amber-100 px-3 py-1.5 text-[10px] font-extrabold text-amber-800"><Clock3 className="size-3" /> Нужен ответ</span></div><div className="mt-5 grid gap-3 border-t pt-4 text-xs sm:grid-cols-3"><div><p className="text-muted-foreground">Отправлена</p><p className="mt-1 font-bold">12 мая 2026</p></div><div><p className="text-muted-foreground">Участники</p><p className="mt-1 font-bold">3 человека</p></div><div><p className="text-muted-foreground">Следующий шаг</p><p className="mt-1 font-bold">Выбрать время просмотра</p></div></div><span className="mt-4 inline-flex items-center gap-2 text-xs font-extrabold">Открыть заявку <ArrowRight className="size-4" /></span></Link><div className="surface-card flex items-start gap-3 p-5"><Sparkles className="mt-0.5 size-5 text-[hsl(var(--accent-hover))]" /><div><p className="text-sm font-extrabold">Совет Соседей</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Добавьте ещё один объект в сравнение — так у группы будет запасной вариант.</p></div></div></div></PageFrame>; }
