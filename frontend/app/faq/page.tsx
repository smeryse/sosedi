import Link from "next/link";
import { ArrowLeft, ChevronDown } from "lucide-react";
import { BrandLogo } from "@/components/brand-logo";

const questions = [["Как считается совместимость?", "Мы сравниваем бюджет, даты, районы и бытовые привычки. Жёсткие конфликты показываем отдельно, а мягкие различия объясняем."], ["Можно ли искать только жильё?", "Да. Каталог доступен без заполненной анкеты, но точность группового match будет ниже."], ["Что происходит после заявки?", "Собственник получает состав группы и сообщение. Ответ и просмотр появляются в разделе заявок."], ["Мои контакты видны всем?", "Нет. Личные контакты открываются только после взаимного согласия в переписке."]];
export default function FaqPage() { return <main className="min-h-screen bg-surface-muted/50"><header className="mx-auto flex h-[76px] max-w-5xl items-center justify-between px-5"><BrandLogo /><Link href="/" className="inline-flex items-center gap-2 text-xs font-bold text-muted-foreground"><ArrowLeft className="size-4" /> На главную</Link></header><article className="mx-auto max-w-3xl px-5 pb-20 pt-12"><p className="text-[11px] font-extrabold uppercase tracking-[0.12em] text-[hsl(var(--accent-hover))]">FAQ</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-0.06em]">Частые вопросы</h1><div className="mt-8 divide-y overflow-hidden rounded-[22px] border bg-surface">{questions.map(([title, answer]) => <details key={title} className="group p-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-3 text-sm font-extrabold"><span>{title}</span><ChevronDown className="size-4 transition-transform group-open:rotate-180" /></summary><p className="mt-3 max-w-2xl text-sm leading-6 text-muted-foreground">{answer}</p></details>)}</div></article></main>; }
