import { PageFrame } from "@/components/tenant/page-frame";

export default async function OwnerPropertyEditPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <PageFrame backHref={`/owner/properties/${id}`} title="Редактировать объект" description="Обновите условия — изменения появятся после сохранения."><form className="max-w-2xl space-y-4"><div className="surface-card grid gap-4 p-5"><label className="text-xs font-extrabold">Название<input defaultValue="Светлая квартира в центре" className="mt-2 h-11 w-full rounded-[14px] border bg-background px-3 text-sm font-normal outline-none" /></label><label className="text-xs font-extrabold">Цена в месяц<input defaultValue="45000" type="number" className="mt-2 h-11 w-full rounded-[14px] border bg-background px-3 text-sm font-normal outline-none" /></label><label className="text-xs font-extrabold">Описание<textarea defaultValue="Светлое жильё с отдельными комнатами и рабочими местами." rows={5} className="mt-2 w-full rounded-[14px] border bg-background p-3 text-sm font-normal outline-none" /></label></div><button type="submit" className="lime-button rounded-full px-5 py-3 text-xs font-extrabold">Сохранить изменения</button></form></PageFrame>; }
