import Link from "next/link";
import { Bell, ChevronRight, LockKeyhole, LogOut, Settings2, UserRound } from "lucide-react";
import { PageFrame } from "@/components/tenant/page-frame";

const settings = [["Профиль и анкета", "Изменить бюджет, районы и привычки", UserRound, "/app/profile"], ["Уведомления", "Настроить email и push-уведомления", Bell, "/app/notifications"], ["Безопасность", "Пароль, подтверждения и сессии", LockKeyhole, "/auth/update-password"], ["Внешний вид", "Светлая тема и язык интерфейса", Settings2, "#"]] as const;
export default function SettingsPage() { return <PageFrame eyebrow="Аккаунт" title="Настройки" description="Управляйте профилем и тем, как Соседи с вами общаются."><div className="max-w-2xl overflow-hidden rounded-[20px] border bg-surface">{settings.map(([title, description, Icon, href]) => <Link href={href} key={title} className="flex items-center gap-4 border-b p-5 last:border-0 hover:bg-surface-muted"><div className="grid size-10 place-items-center rounded-full bg-surface-muted"><Icon className="size-4" /></div><div className="min-w-0 flex-1"><p className="text-sm font-extrabold">{title}</p><p className="mt-1 text-xs text-muted-foreground">{description}</p></div><ChevronRight className="size-4 text-muted-foreground" /></Link>)}</div><button type="button" className="mt-5 inline-flex items-center gap-2 rounded-full border px-4 py-3 text-xs font-extrabold text-red-600"><LogOut className="size-4" /> Выйти из аккаунта</button></PageFrame>; }
