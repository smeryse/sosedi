import Link from "next/link";
import { ArrowLeft, Check, Minus, Sparkles } from "lucide-react";
import { demoRoommates } from "@/data/demo";
import { PageFrame } from "@/components/tenant/page-frame";

const rows = [["Бюджет", "25 000 ₽", "28 000 ₽", "30 000 ₽"], ["Район", "Центр", "Фестивальный", "Юбилейный"], ["Режим", "Ранний", "Гибкий", "Ранний"], ["Гости", "Редко", "Иногда", "Редко"], ["Совместимость", "96%", "94%", "93%"]];

export default function ComparePage() { return <PageFrame backHref="/app/roommates" title="Сравнение соседей" description="Смотрите важные различия рядом и выбирайте тех, с кем проще договориться."><div className="overflow-x-auto rounded-[22px] border bg-surface"><div className="min-w-[680px]"><div className="grid grid-cols-[190px_repeat(3,1fr)] border-b bg-surface-muted p-4 text-xs font-extrabold"><span>Критерий</span>{demoRoommates.slice(0, 3).map((person) => <span key={person.id}>{person.name}, {person.age}</span>)}</div>{rows.map(([label, ...values]) => <div key={label} className="grid grid-cols-[190px_repeat(3,1fr)] items-center border-b p-4 text-sm last:border-0"><span className="font-bold">{label}</span>{values.map((value, index) => <span key={`${label}-${value}`} className={label === "Совместимость" ? "font-extrabold text-[hsl(var(--accent-hover))]" : "text-muted-foreground"}>{label === "Совместимость" ? <span className="inline-flex items-center gap-1"><Sparkles className="size-3" /> {value}</span> : value}{label === "Режим" && index === 1 ? <Check className="ml-1 inline size-3 text-[hsl(var(--accent-hover))]" /> : null}</span>)}</div>)}</div></div><div className="grid gap-4 md:grid-cols-2"><div className="surface-card p-5"><h2 className="font-extrabold">Что обсудить</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">У Марии и Екатерины одинаковый режим сна, но разные районы. Сначала уточните, насколько гибкая география.</p><Link href="/app/assistant" className="mt-4 inline-flex items-center gap-2 text-xs font-extrabold">Спросить AI-помощника →</Link></div><div className="surface-card p-5"><h2 className="font-extrabold">Решили?</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Пригласите соседа в группу, чтобы вместе откликаться на квартиры.</p><Link href="/app/group" className="lime-button mt-4 inline-flex items-center gap-2 rounded-full px-4 py-3 text-xs font-extrabold"><ArrowLeft className="size-4 rotate-180" /> Открыть группу</Link></div></div><div className="flex items-center gap-2 text-xs text-muted-foreground"><Minus className="size-3" /> Демонстрационные данные можно заменить вашей анкетой.</div></PageFrame>; }
