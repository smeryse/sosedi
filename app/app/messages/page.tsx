import Link from "next/link";
import { MessageCircle, Search } from "lucide-react";
import { PageFrame } from "@/components/tenant/page-frame";

const chats = [{ id: "maria", name: "Мария", preview: "Давайте обсудим просмотр в четверг", time: "12:40", unread: 2 }, { id: "group", name: "Квартира в центре", preview: "Артём: Я обновил бюджет группы", time: "Вчера", unread: 1 }, { id: "owner", name: "Собственник квартиры", preview: "Готовы показать объект после 18:00", time: "12 мая", unread: 0 }];
export default function MessagesPage() { return <PageFrame eyebrow="Общение" title="Сообщения" description="Переписка с соседями, группой и собственниками — в одном месте."><div className="mb-4 flex h-11 items-center gap-2 rounded-full border bg-surface px-4 text-muted-foreground"><Search className="size-4" /><input placeholder="Поиск в сообщениях" className="min-w-0 flex-1 bg-transparent text-sm outline-none" /></div><div className="divide-y overflow-hidden rounded-[20px] border bg-surface">{chats.map((chat) => <Link href={`/app/messages/${chat.id}`} key={chat.id} className="flex items-center gap-3 p-4 transition-colors hover:bg-surface-muted"><div className="grid size-11 shrink-0 place-items-center rounded-full bg-[hsl(var(--accent-soft))] text-sm font-extrabold">{chat.name.slice(0, 1)}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-3"><p className="truncate text-sm font-extrabold">{chat.name}</p><span className="text-[10px] text-muted-foreground">{chat.time}</span></div><p className="mt-1 truncate text-xs text-muted-foreground">{chat.preview}</p></div>{chat.unread ? <span className="grid size-5 place-items-center rounded-full bg-[hsl(var(--accent))] text-[10px] font-extrabold">{chat.unread}</span> : <MessageCircle className="size-4 text-muted-foreground" />}</Link>)}</div></PageFrame>; }
