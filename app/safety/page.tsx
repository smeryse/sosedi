import Link from "next/link";
import { ArrowLeft, Check, LockKeyhole, ShieldCheck } from "lucide-react";
import { BrandLogo } from "@/components/brand-logo";

export default function SafetyPage() { return <main className="min-h-screen bg-surface-muted/50"><header className="mx-auto flex h-[76px] max-w-5xl items-center justify-between px-5"><BrandLogo /><Link href="/" className="inline-flex items-center gap-2 text-xs font-bold text-muted-foreground"><ArrowLeft className="size-4" /> На главную</Link></header><article className="mx-auto max-w-3xl px-5 pb-20 pt-12"><p className="text-[11px] font-extrabold uppercase tracking-[0.12em] text-[hsl(var(--accent-hover))]">Безопасность</p><h1 className="mt-3 text-4xl font-extrabold tracking-[-0.06em]">Доверие складывается из маленьких правил.</h1><div className="mt-8 space-y-3">{[["Контакты скрыты", "Email и телефон не видны другим пользователям до взаимного согласия.", LockKeyhole], ["Профили подтверждаются", "Мы показываем статус подтверждения и не выдаём непроверенные обещания за факт.", ShieldCheck], ["Вы контролируете общение", "Можно пожаловаться, заблокировать пользователя или выйти из группы в любой момент.", Check]].map(([title, description, Icon]) => <section key={title as string} className="surface-card flex gap-4 p-5"><div className="grid size-10 shrink-0 place-items-center rounded-full bg-[hsl(var(--accent-soft))]"><Icon className="size-4" /></div><div><h2 className="font-extrabold">{title as string}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{description as string}</p></div></section>)}</div></article></main>; }
