import { Bell, LockKeyhole, Settings2 } from "lucide-react";
import { PageFrame } from "@/components/tenant/page-frame";

export default function OwnerSettingsPage() { return <PageFrame title="Настройки" description="Уведомления и безопасность кабинета собственника."><div className="max-w-2xl space-y-3">{[["Уведомления о заявках", "Получать письмо о новых откликах", Bell], ["Быстрые ответы", "Сохранять шаблоны сообщений", Settings2], ["Безопасность", "Подтверждение телефона и активные сессии", LockKeyhole]].map(([title, description, Icon]) => <div key={title as string} className="surface-card flex items-center gap-4 p-5"><div className="grid size-10 place-items-center rounded-full bg-surface-muted"><Icon className="size-4" /></div><div className="min-w-0 flex-1"><p className="text-sm font-extrabold">{title as string}</p><p className="mt-1 text-xs text-muted-foreground">{description as string}</p></div><div className="h-6 w-11 rounded-full bg-[hsl(var(--accent))] p-1"><div className="ml-auto size-4 rounded-full bg-foreground" /></div></div>)}</div></PageFrame>; }
