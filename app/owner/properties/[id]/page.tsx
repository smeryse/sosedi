import Link from "next/link";
import { Edit3, Eye, PauseCircle, UsersRound } from "lucide-react";
import { PageFrame } from "@/components/tenant/page-frame";
import { demoProperties, formatRubles } from "@/data/demo";
import { notFound } from "next/navigation";

export default async function OwnerPropertyPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const property = demoProperties.find((item) => item.id === id); if (!property) notFound(); return <PageFrame backHref="/owner/properties" title={property.title} description="Управление публикацией и откликами по объекту."><div className="grid gap-5 lg:grid-cols-[minmax(0,1fr)_300px]"><section className="surface-card p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs text-muted-foreground">{property.district} · {property.rooms} комнаты · {formatRubles(property.price)}</p><h2 className="mt-2 text-xl font-extrabold">Публикация активна</h2></div><span className="rounded-full bg-[hsl(var(--accent-soft))] px-3 py-1.5 text-[10px] font-extrabold">Опубликован</span></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[["Просмотры", "48", Eye], ["Сохранения", "12", UsersRound], ["Заявки", "3", UsersRound]].map(([label, value, Icon]) => <div key={label as string} className="rounded-[15px] bg-surface-muted p-4"><Icon className="size-4" /><p className="mt-4 text-2xl font-extrabold">{value as string}</p><p className="mt-1 text-[10px] text-muted-foreground">{label as string}</p></div>)}</div><div className="mt-6 flex flex-wrap gap-2"><Link href={`/owner/properties/${property.id}/edit`} className="inline-flex items-center gap-2 rounded-full bg-[hsl(var(--accent))] px-4 py-2.5 text-xs font-extrabold"><Edit3 className="size-4" /> Редактировать</Link><Link href={`/app/housing/${property.id}`} className="inline-flex items-center gap-2 rounded-full border px-4 py-2.5 text-xs font-extrabold"><Eye className="size-4" /> Смотреть как гость</Link><button type="button" className="inline-flex items-center gap-2 rounded-full border px-4 py-2.5 text-xs font-extrabold"><PauseCircle className="size-4" /> Приостановить</button></div></section><aside className="surface-card p-5"><h2 className="font-extrabold">Следующие заявки</h2><p className="mt-2 text-xs leading-5 text-muted-foreground">3 группы ждут вашего ответа. Быстрый ответ повышает доверие.</p><Link href="/owner/applications" className="mt-5 inline-flex items-center gap-2 text-xs font-extrabold">Открыть заявки →</Link></aside></div></PageFrame>; }
